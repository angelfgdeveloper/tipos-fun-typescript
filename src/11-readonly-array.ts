const numbers: ReadonlyArray<number> = [1,2,3,4];
// numbers.push(5); // agrega
// numbers.pop(); // elimina el ultimo elemento
// numbers.unshift(9); // insertar al inicio

numbers.filter(() => {});
numbers.reduce(() => 0);
numbers.map(() => 0);
